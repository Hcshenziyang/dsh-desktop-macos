# Changelog

本文件记录 DSH Desktop Community 的公开版本变化。项目从 `v0.1.0` 起采用语义化版本号。

## 0.1.2 - 2026-09-02

### 新增

- 独立主题与壁纸模块：提供版本化配置、可扩展预设注册表、DSH 浅/深语义令牌覆盖、自定义
  强调色、静态壁纸及实时模糊、暗化、面板透明度调节。
- 壁纸复制到应用的私有 Application Support 目录，并通过受限 WKWebView URL Scheme
  读取，不暴露原始图片路径，也不修改 DSH 核心或 profile。
- 新增十一套相互隔离、数据驱动的原创完整皮肤，覆盖二次元、简约、可爱、风景、赛博朋克、
  可爱猫娘、日系写真、民俗诡谲、龙与魔法、黑暗史诗和像素方块；每套独立携带清单、16:10
  壁纸和素材来源说明。
- 主题清单升级到 schema v3，在圆角、玻璃模糊、图标语言、光晕与环境动效之外，增加独立
  `frame` 框体参数；内置软边、霓虹、缝线、花影、诡箓、秘法双线、风化和像素硬边。
- 新增主题化原生确认框、错误/成功状态卡片和工具栏快捷图标；十一种环境动效同时覆盖
  SwiftUI 外壳与内嵌 Web UI，并遵循系统“减少动态效果”设置。
- 会话历史与工作区浏览器升级为主题卡片：包含分组外框、轮换装饰图标、选中/悬停层、主题化
  搜索框和“新会话”入口；搜索结果、选项列表与插件卡片共享较轻的同类反馈。

### 调整

- System Prompt、Memory Profile 与系统提示组成段改为随窗口宽度自动换行。
- 本地模型入口改为紧凑图标按钮，悬停时显示模型名称、状态和当前动作。
- 主题覆盖从基础背景色扩展到侧栏、气泡、输入框、按钮、交互态、文字、代码块、滚动条、
  阴影、字体、弹窗、菜单、状态提示，以及 SwiftUI 原生工具栏和管理窗口。
- 会话卡片、工作区、搜索结果、菜单、输入区、代码块、状态提示和确认弹窗会按主题切换独立
  边框结构、装饰符号、投影与悬停动画。

### 修复

- 本地模型健康检查不再把任意 HTTP 响应都视为成功；`4xx`/`5xx` 会提示端口可能被其他服务
  占用，运行中模型连续探测失败也会从“已就绪”转为明确异常。
- 主题与壁纸弹窗的“完成”按钮改为直接更新父界面的展示状态，确保可以可靠关闭。
- 修复部分页面中自选壁纸被 `body` 背景层遮住、导入后几乎不可见的问题。
- 修复主题状态属性值与 CSS 选择器不一致，导致 Web 配色和壁纸覆盖完全没有应用的问题。

### 移除

- 移除工具栏中的“系统浏览器”按钮；DSH Web UI 统一在客户端内嵌窗口中使用。
- 移除“绯夜主场”和“青柚晨光”两套人物主题及其独立素材目录。

## 0.1.0 - 2026-09-01

首个独立维护的 macOS 社区版本。

### 新增

- 使用 SwiftUI 与 WKWebView 构建的 macOS 原生客户端，同时支持 Apple Silicon 与 Intel。
- DSH 服务的发现、启动、停止、重启、日志查看和浏览器打开功能。
- 可配置的本地模型服务启动与停止入口，不绑定具体模型或启动脚本。
- 原生归档管理，可查看、恢复和安全清理已归档会话。
- 内嵌插件列表简化视图，将用户插件和异常单元置于前台。
- “模型固定输入与长期记忆”检查器，可只读查看最终 System Prompt、工具目录、Skill
  目录、工作区指令和 `dsh-native-memory` 长期记忆。
- 自包含的通用 App 构建、ZIP/DMG 打包、持续集成和标签发布流程。

### 调整

- 项目范围收敛为 macOS，移除原仓库中的 Android、iOS 和 Windows 客户端。
- 设置窗口改为固定标题与底部操作区、中部滚动布局，小窗口下仍能访问完整内容。
- 使用独立的应用名称、Bundle ID、版本、仓库与发布说明，同时完整保留上游 MIT
  许可、版权和来源声明。

### 已知限制

- 当前公开构建采用 ad-hoc 签名；面向普通用户分发前仍建议使用 Developer ID 签名并公证。
- 固定输入检查器只会自动观察由本客户端启动的 DSH，外部 DSH 实例需要先停止再由客户端启动。
- 插件列表简化依赖 DSH Web 当前公开的 DOM 属性；上游结构变化时会安全回退到原始列表。
